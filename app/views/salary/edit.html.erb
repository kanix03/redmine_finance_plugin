<%= form_for @salary, :url => edit_salary_path() do |f| %>
<% if @salary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@salary.errors.count, "error") %> prohibited this expense from being saved:</h2>
      <ul>
      <% @salary.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="box tabular">
    <div id="all_attributes">
	<div id="attributes" class="attributes">
		<div id="company" class="splitcontent">
 <p>
    <%= f.label :valid_from,l(:salary_valid_from) %><%= calendar_for('salary_valid_from') %>
    <%= f.text_field :valid_from %>
  </p>
  <p>
    <%= f.label :valid_to,l(:salary_valid_to) %><%= calendar_for('salary_valid_to') %>
    <%= f.text_field :valid_to %>
  </p>
  <p>
    <%= f.label :hours_rate,l(:salary_hours_rate) %>
    <%= f.text_field :hours_rate %>
  </p>
		</div>
	</div>
    </div>

</div>
<div class="actions">
    <%= f.submit l(:update) %>
  </div>
<% end %>
